<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Superhéroes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold text-gray-700 text-center mb-4">Lista de Superhéroes</h1>
        <table class="w-full border-collapse border border-gray-300">
            <thead>
                <tr class="bg-gray-200">
                    <th class="border border-gray-300 px-4 py-2">ID</th>
                    <th class="border border-gray-300 px-4 py-2">Nombre</th>
                    <th class="border border-gray-300 px-4 py-2">Poder</th>
                </tr>
            </thead>
            <tbody>
                <% heroes.forEach(hero => { %>
                    <tr class="hover:bg-gray-100">
                        <td class="border border-gray-300 px-4 py-2"><%= hero._id %></td>
                        <td class="border border-gray-300 px-4 py-2"><%= hero.nombreSuperHeroe %></td>
                        <td class="border border-gray-300 px-4 py-2"><%= hero.poderes %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <a href="/api/createhero" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">Agregar Superhéroe</a>
</body>
</html>